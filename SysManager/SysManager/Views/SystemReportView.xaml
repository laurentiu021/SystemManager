<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.SystemReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             d:DataContext="{d:DesignInstance vm:SystemReportViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource Surface0}">

    <Grid Margin="28,24,28,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="System Report" Style="{StaticResource Display}"/>
            <TextBlock Text="Generate a full snapshot of this PC — OS, CPU, memory, GPU, motherboard, storage health, and network — then export it as text, HTML, or JSON. Read-only: nothing is changed and nothing leaves this machine."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Toolbar -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <WrapPanel>
                <Button Content="Generate" Command="{Binding GenerateCommand}"
                        AutomationProperties.Name="Generate system report"
                        Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"
                        ToolTip="Gather a fresh system snapshot."/>
                <Button Content="Cancel" Command="{Binding CancelCommand}"
                        AutomationProperties.Name="Cancel report generation"
                        Style="{StaticResource GhostButton}" Margin="0,0,16,0"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>

                <Button Content="Export HTML" Command="{Binding ExportHtmlCommand}"
                        AutomationProperties.Name="Export report as HTML"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                        ToolTip="Save a styled, self-contained HTML report."/>
                <Button Content="Export JSON" Command="{Binding ExportJsonCommand}"
                        AutomationProperties.Name="Export report as JSON"
                        Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                        ToolTip="Save a structured JSON report."/>
                <Button Content="Export text" Command="{Binding ExportTextCommand}"
                        AutomationProperties.Name="Export report as text"
                        Style="{StaticResource GhostButton}" Margin="0,0,8,0"
                        ToolTip="Save the plain-text report."/>
                <Button Content="Copy" Command="{Binding CopyCommand}"
                        AutomationProperties.Name="Copy report to clipboard"
                        Style="{StaticResource GhostButton}"
                        ToolTip="Copy the plain-text report to the clipboard."/>
            </WrapPanel>
        </Border>

        <!-- Report preview -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="16,12">
                    <TextBox Text="{Binding ReportText, Mode=OneWay}"
                             AutomationProperties.Name="System report preview"
                             IsReadOnly="True" TextWrapping="NoWrap"
                             Background="Transparent" BorderThickness="0"
                             FontFamily="{StaticResource MonoFont}" FontSize="12"
                             Foreground="{DynamicResource TextPrimary}"
                             Visibility="{Binding HasReport, Converter={StaticResource FlexVis}}"/>
                </ScrollViewer>

                <!-- Empty state -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding HasReport, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
                    <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="32"
                               HorizontalAlignment="Center" Margin="0,0,0,8"
                               Foreground="{DynamicResource TextSecondary}"/>
                    <TextBlock Text="No report yet" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondary}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Click Generate to build a full system snapshot."
                               Style="{StaticResource Subtle}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Status bar -->
        <DockPanel Grid.Row="3" Margin="0,8,0,0">
            <ProgressBar AutomationProperties.Name="Progress" DockPanel.Dock="Right" Width="180" Height="4"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Visibility="{Binding IsBusy, Converter={StaticResource FlexVis}}"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}" VerticalAlignment="Center"/>
        </DockPanel>
    </Grid>
</UserControl>
