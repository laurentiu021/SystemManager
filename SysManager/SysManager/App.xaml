<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<Application x:Class="SysManager.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:h="clr-namespace:SysManager.Helpers"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Dark" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <!-- ============================================================ -->
            <!-- Converters -->
            <!-- ============================================================ -->
            <BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <h:FlexibleBoolToVisibilityConverter x:Key="FlexVis"/>
            <h:BoolInverterConverter x:Key="BoolInvert"/>
            <h:HexToBrushConverter x:Key="HexBrush"/>
            <h:OutputKindToBrushConverter x:Key="KindBrush"/>
            <h:ProcessStatusToBrushConverter x:Key="StatusBrush"/>
            <h:SafetyLevelToBrushConverter x:Key="SafetyBrush"/>
            <h:SafetyLevelToBackgroundConverter x:Key="SafetyBg"/>
            <h:SafetyLevelToTextConverter x:Key="SafetyText"/>
            <h:IntGreaterThanZeroConverter x:Key="GreaterThanZero"/>
            <h:EqualityConverter x:Key="IsEqual"/>

            <sys:Boolean xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="TrueValue">True</sys:Boolean>
            <sys:Boolean xmlns:sys="clr-namespace:System;assembly=mscorlib" x:Key="FalseValue">False</sys:Boolean>

            <!-- ============================================================ -->
            <!-- Color palette — 2026 dark design                              -->
            <!-- ============================================================ -->
            <!-- Surfaces (layered backgrounds, darkest → lightest)            -->
            <Color x:Key="Surface0Color">#070A0F</Color>
            <Color x:Key="Surface1Color">#0E1218</Color>
            <Color x:Key="Surface2Color">#151A23</Color>
            <Color x:Key="Surface3Color">#1C2230</Color>
            <Color x:Key="Surface4Color">#252C3C</Color>

            <SolidColorBrush x:Key="Surface0" Color="{DynamicResource Surface0Color}"/>
            <SolidColorBrush x:Key="Surface1" Color="{DynamicResource Surface1Color}"/>
            <SolidColorBrush x:Key="Surface2" Color="{DynamicResource Surface2Color}"/>
            <SolidColorBrush x:Key="Surface3" Color="{DynamicResource Surface3Color}"/>
            <SolidColorBrush x:Key="Surface4" Color="{DynamicResource Surface4Color}"/>

            <!-- Borders -->
            <SolidColorBrush x:Key="Border1" Color="#1F2633"/>
            <SolidColorBrush x:Key="Border2" Color="#2A3244"/>
            <SolidColorBrush x:Key="BorderAccent" Color="#3B4A66"/>

            <!-- Card depth brushes — static dark-preset fallbacks so cards never flash a null brush
                 before ThemeService.Apply() replaces them per preset at startup (see ThemeService.Apply). -->
            <LinearGradientBrush x:Key="CardSurface" StartPoint="0.5,0" EndPoint="0.5,1">
                <GradientStop Color="#12161E" Offset="0"/>
                <GradientStop Color="#0E1218" Offset="0.55"/>
            </LinearGradientBrush>
            <LinearGradientBrush x:Key="CardRim" StartPoint="0.5,0" EndPoint="0.5,1">
                <GradientStop Color="#33405A" Offset="0"/>
                <GradientStop Color="#1F2633" Offset="0.5"/>
            </LinearGradientBrush>
            <!-- Subtle neutral row-hover, distinct from accent selection. Dark fallback; ThemeService derives per preset. -->
            <SolidColorBrush x:Key="RowHover" Color="#1A1F2A"/>

            <!-- Text -->
            <SolidColorBrush x:Key="TextPrimary" Color="#F1F3F7"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#A3ADBF"/>
            <SolidColorBrush x:Key="TextMuted" Color="#7B8396"/>
            <SolidColorBrush x:Key="TextDisabled" Color="#4A5266"/>

            <!-- Accent (primary action) -->
            <Color x:Key="AccentColor">#6366F1</Color>
            <Color x:Key="AccentHoverColor">#818CF8</Color>
            <Color x:Key="AccentPressedColor">#4F46E5</Color>
            <SolidColorBrush x:Key="Accent" Color="{DynamicResource AccentColor}"/>
            <SolidColorBrush x:Key="AccentHover" Color="{DynamicResource AccentHoverColor}"/>
            <SolidColorBrush x:Key="AccentPressed" Color="{DynamicResource AccentPressedColor}"/>
            <SolidColorBrush x:Key="AccentSoft" Color="#186366F1"/>

            <!-- Status colors -->
            <SolidColorBrush x:Key="Success" Color="#22C55E"/>
            <SolidColorBrush x:Key="Warning" Color="#F59E0B"/>
            <SolidColorBrush x:Key="Danger" Color="#EF4444"/>
            <SolidColorBrush x:Key="Info" Color="#38BDF8"/>

            <!-- Dashboard metric identity colors (CPU=Success green, MEM=blue, GPU=purple).
                 Named so the metric cards reference brushes instead of copy-pasting the same hex;
                 the mid-tone blue/purple stay legible on both dark and light presets. -->
            <SolidColorBrush x:Key="MetricBlue" Color="#3B82F6"/>
            <SolidColorBrush x:Key="MetricPurple" Color="#A855F7"/>

            <!-- Warning category surface (admin elevation banner, etc.) -->
            <SolidColorBrush x:Key="WarningBgSubtle" Color="#1AFBBF24"/>
            <SolidColorBrush x:Key="WarningBg" Color="#40FBBF24"/>
            <SolidColorBrush x:Key="WarningStripe" Color="#FBBF24"/>
            <SolidColorBrush x:Key="WarningText" Color="#FCD34D"/>

            <!-- Info / Success / Danger category surfaces + text. These defaults are dark-calibrated;
                 ThemeService.ApplyStatusBrushes() recomputes ALL of them per preset so light themes
                 get WCAG-AA-legible text (pale dark-theme text washes out on near-white surfaces). -->
            <SolidColorBrush x:Key="InfoBgSubtle" Color="#1A38BDF8"/>
            <SolidColorBrush x:Key="InfoBorder" Color="#3338BDF8"/>
            <SolidColorBrush x:Key="InfoText" Color="#7DD3FC"/>
            <SolidColorBrush x:Key="SuccessBgSubtle" Color="#1A22C55E"/>
            <SolidColorBrush x:Key="SuccessBorder" Color="#3322C55E"/>
            <SolidColorBrush x:Key="SuccessText" Color="#4ADE80"/>
            <SolidColorBrush x:Key="DangerBgSubtle" Color="#1AEF4444"/>
            <SolidColorBrush x:Key="DangerBorder" Color="#33EF4444"/>
            <SolidColorBrush x:Key="DangerText" Color="#F87171"/>

            <!-- Console output coloring (legacy) -->
            <SolidColorBrush x:Key="OutErrorBrush"   Color="#F87171" />
            <SolidColorBrush x:Key="OutWarnBrush"    Color="#FBBF24" />
            <SolidColorBrush x:Key="OutInfoBrush"    Color="#38BDF8" />
            <SolidColorBrush x:Key="OutVerboseBrush" Color="#9AA0A6" />
            <SolidColorBrush x:Key="OutDebugBrush"   Color="#B388FF" />
            <SolidColorBrush x:Key="OutProgressBrush" Color="#4ADE80" />
            <SolidColorBrush x:Key="OutOutputBrush"  Color="#E6E6E6" />

            <FontFamily x:Key="MonoFont">Cascadia Mono, Consolas, Courier New</FontFamily>
            <FontFamily x:Key="UiFont">Segoe UI Variable Text, Segoe UI, Inter, Arial</FontFamily>

            <!-- ============================================================ -->
            <!-- Design tokens — radius / spacing / motion (single source)     -->
            <!-- Radius scale 4/8/12/16/999 · spacing 4px grid · motion         -->
            <!-- 150/200/300ms. These do NOT vary per preset, so StaticResource -->
            <!-- (cheap, no theme-swap). Colors stay DynamicResource. This kills -->
            <!-- the per-style radius drift (was 5/6/8/10/999 scattered).        -->
            <!-- ============================================================ -->
            <CornerRadius x:Key="RadiusSm">4</CornerRadius>
            <CornerRadius x:Key="RadiusMd">8</CornerRadius>
            <CornerRadius x:Key="RadiusLg">12</CornerRadius>
            <CornerRadius x:Key="RadiusXl">16</CornerRadius>
            <CornerRadius x:Key="RadiusPill">999</CornerRadius>

            <!-- Spacing rhythm on a 4px base. Defined here as the single source;
                 the per-view rollout migrates raw margins/paddings to these. -->
            <Thickness x:Key="Space4">4</Thickness>
            <Thickness x:Key="Space8">8</Thickness>
            <Thickness x:Key="Space12">12</Thickness>
            <Thickness x:Key="Space16">16</Thickness>
            <Thickness x:Key="Space24">24</Thickness>
            <Thickness x:Key="Space32">32</Thickness>

            <!-- Motion vocabulary: Fast=hover/focus/selection, Mid=view transitions,
                 Slow=theme-background fade. ease-out, no bounce (see brief §1.5). -->
            <Duration x:Key="DurFast">0:0:0.15</Duration>
            <Duration x:Key="DurMid">0:0:0.2</Duration>
            <Duration x:Key="DurSlow">0:0:0.3</Duration>

            <!-- ============================================================ -->
            <!-- Sidebar Expander — smooth slide animation                      -->
            <!-- ============================================================ -->
            <Style x:Key="SidebarExpander" TargetType="Expander">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <DockPanel LastChildFill="True">
                                <ToggleButton x:Name="HeaderSite" DockPanel.Dock="Top"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              Background="Transparent" BorderThickness="0"
                                              HorizontalAlignment="Stretch"
                                              HorizontalContentAlignment="Stretch"
                                              Cursor="Hand" Padding="0" Margin="0"
                                              Focusable="False">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <ContentPresenter HorizontalAlignment="Stretch"/>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Border x:Name="ContentPanel" ClipToBounds="True">
                                    <ContentPresenter x:Name="ExpanderContent"
                                                      ContentSource="Content"
                                                      Margin="{TemplateBinding Padding}"/>
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleY="0"/>
                                    </Border.RenderTransform>
                                    <Border.LayoutTransform>
                                        <ScaleTransform ScaleY="0"/>
                                    </Border.LayoutTransform>
                                </Border>
                            </DockPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ContentPanel"
                                                                 Storyboard.TargetProperty="(Border.LayoutTransform).(ScaleTransform.ScaleY)"
                                                                 From="0" To="1" Duration="0:0:0.15"
                                                                 EasingFunction="{x:Null}"/>
                                                <DoubleAnimation Storyboard.TargetName="ContentPanel"
                                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                 From="0" To="1" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="ContentPanel"
                                                                 Storyboard.TargetProperty="(Border.LayoutTransform).(ScaleTransform.ScaleY)"
                                                                 From="1" To="0" Duration="0:0:0.12"/>
                                                <DoubleAnimation Storyboard.TargetName="ContentPanel"
                                                                 Storyboard.TargetProperty="(Border.RenderTransform).(ScaleTransform.ScaleY)"
                                                                 From="1" To="0" Duration="0:0:0.12"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Theme popup styles                                             -->
            <!-- ============================================================ -->
            <Style x:Key="ModePill" TargetType="RadioButton">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" Padding="7,6" CornerRadius="{StaticResource RadiusMd}" Background="Transparent"
                                    Cursor="Hand" HorizontalAlignment="Stretch">
                                <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Center" Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Txt" Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="SectionLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <!-- ============================================================ -->
            <!-- Global defaults — every Control gets a modern base look       -->
            <!-- ============================================================ -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
                <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
            </Style>

            <Style TargetType="ProgressBar">
                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>

            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Style.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="{DynamicResource AccentColor}"/>
                </Style.Resources>
            </Style>

            <!-- ============================================================ -->
            <!-- Card — reusable container                                      -->
            <!-- ============================================================ -->
            <!-- Card — the primary content surface. Background + border are theme-derived depth
                 brushes (CardSurface = top sheen fading to Surface1; CardRim = top-lit hairline
                 fading to Border1), built by ThemeService per preset. Pure gradients, no shadow
                 (PERF-008-safe on repeated cards). This is the revamp's signature "glass depth". -->
            <Style x:Key="Card" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource CardSurface}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource CardRim}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="{StaticResource RadiusLg}"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
            </Style>

            <!-- PERF-008: CardElevated kept without DropShadowEffect to avoid
                 software-rendered shadows when many cards are visible. -->
            <Style x:Key="CardElevated" TargetType="Border" BasedOn="{StaticResource Card}">
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
            </Style>

            <Style x:Key="CardInner" TargetType="Border" BasedOn="{StaticResource Card}">
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
                <Setter Property="CornerRadius" Value="{StaticResource RadiusMd}"/>
                <Setter Property="Padding" Value="12"/>
            </Style>

            <!-- ============================================================ -->
            <!-- Buttons                                                       -->
            <!-- ============================================================ -->
            <Style x:Key="ButtonBase" TargetType="Button">
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource RadiusMd}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource BorderAccent}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Primary: accent background -->
            <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
                <Setter Property="Background" Value="{DynamicResource Accent}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="White"/>
            </Style>

            <!-- Secondary: subtle surface with border -->
            <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
                <Setter Property="Background" Value="{DynamicResource Surface3}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border2}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>

            <!-- Ghost: subtle border, highlighted on hover -->
            <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <!-- Border uses the theme-adaptive Border1/Border2 brushes (updated per
                                 preset by ThemeService.Apply) instead of hardcoded translucent white,
                                 which was invisible on the six light presets (white border on white). -->
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="{StaticResource RadiusMd}"
                                    Padding="{TemplateBinding Padding}"
                                    BorderThickness="1" BorderBrush="{DynamicResource Border1}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface3}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Border2}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface4}"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface3}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Border2}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- ToggleSwitch — purple pill toggle (on/off/locked)             -->
            <!-- ============================================================ -->
            <Style x:Key="ToggleSwitch" TargetType="ToggleButton">
                <Setter Property="Width" Value="40"/>
                <Setter Property="Height" Value="22"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Grid>
                                <!-- OFF track uses a NEUTRAL surface tone, not TextMuted: on the light
                                     presets TextMuted is an accent-family tint (tuned for AA body text),
                                     so it collided with the ON Accent and the on/off states looked alike.
                                     Surface4 stays a neutral grey in both modes, keeping OFF clearly distinct. -->
                                <!-- BorderThickness reserved at 1.5 (transparent) so the IsKeyboardFocused
                                     trigger can light the Accent ring without shifting the thumb/track layout. -->
                                <Border x:Name="Track" CornerRadius="11" Background="{DynamicResource Surface4}"
                                        BorderThickness="1.5" BorderBrush="Transparent"/>
                                <Border x:Name="Thumb" Width="18" Height="18" CornerRadius="9"
                                        Background="White" HorizontalAlignment="Left" Margin="2,2,0,2"
                                        VerticalAlignment="Center">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="1" BlurRadius="3" Opacity="0.3" Color="Black"/>
                                    </Border.Effect>
                                </Border>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Thumb" Property="Margin" Value="0,2,2,2"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Track" Property="Opacity" Value="0.88"/>
                                </Trigger>
                                <!-- Keyboard focus ring (a11y, WCAG 2.4.7): the other interactive styles
                                     (ButtonBase, Ghost/Admin/Danger buttons, TextBox) all carry this trigger;
                                     ToggleSwitch was missed, so a keyboard user tabbing onto a switch saw no
                                     focus cue while Space still toggled it. Track is a Border, so it takes an
                                     Accent border like the button styles do. -->
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Track" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Filter Chips — safety level radio buttons                    -->
            <!-- ============================================================ -->
            <Style x:Key="FilterChip" TargetType="RadioButton">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" CornerRadius="{StaticResource RadiusPill}" Padding="8,4"
                                    Background="{DynamicResource SuccessBgSubtle}" BorderBrush="{DynamicResource SuccessBorder}" BorderThickness="1"
                                    Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="Dot" Width="6" Height="6" Fill="{DynamicResource Success}" Margin="0,0,5,0"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SuccessBorder}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Success}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Foreground" Value="{DynamicResource SuccessText}"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="FontWeight" Value="Medium"/>
            </Style>

            <Style x:Key="FilterChipCaution" TargetType="RadioButton" BasedOn="{StaticResource FilterChip}">
                <Setter Property="Foreground" Value="{DynamicResource WarningText}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" CornerRadius="{StaticResource RadiusPill}" Padding="8,4"
                                    Background="{DynamicResource WarningBgSubtle}" BorderBrush="{DynamicResource WarningStripe}" BorderThickness="1"
                                    Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="Dot" Width="6" Height="6" Fill="{DynamicResource Warning}" Margin="0,0,5,0"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource WarningBg}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource WarningStripe}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="FilterChipCritical" TargetType="RadioButton" BasedOn="{StaticResource FilterChip}">
                <Setter Property="Foreground" Value="{DynamicResource DangerText}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" CornerRadius="{StaticResource RadiusPill}" Padding="8,4"
                                    Background="{DynamicResource DangerBgSubtle}" BorderBrush="{DynamicResource DangerBorder}" BorderThickness="1"
                                    Cursor="Hand">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="Dot" Width="6" Height="6" Fill="{DynamicResource Danger}" Margin="0,0,5,0"/>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource DangerBorder}"/>
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Danger}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Admin: golden glass button for elevation requests. Uses the theme-aware
                 Warning* brushes (recomputed per preset by ThemeService) so the text stays
                 legible on light presets — hardcoded amber (#FCD34D) washed out on white. -->
            <Style x:Key="AdminButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
                <Setter Property="Foreground" Value="{DynamicResource WarningText}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" CornerRadius="{StaticResource RadiusMd}" Padding="{TemplateBinding Padding}"
                                    Background="{DynamicResource WarningBgSubtle}" BorderBrush="{DynamicResource WarningStripe}" BorderThickness="1">
                                <Grid>
                                    <Border Background="{DynamicResource WarningStripe}" Width="4" HorizontalAlignment="Left" CornerRadius="2"
                                            Margin="-14,-8,0,-8" x:Name="Bar"/>
                                    <StackPanel Orientation="Horizontal" Margin="4,0,0,0">
                                        <TextBlock Text="&#x1F6E1;" FontSize="13" VerticalAlignment="Center"/>
                                        <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"
                                                          TextBlock.Foreground="{DynamicResource WarningText}" TextBlock.FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource WarningStripe}"/>
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource WarningBg}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource WarningBg}"/>
                                </Trigger>
                                <!-- Keyboard focus ring (a11y): AdminButton omitted the IsKeyboardFocused
                                     trigger the other button styles carry, so it had no visible focus cue.
                                     Uses the brighter amber (WarningText) + a thicker border, staying within
                                     the admin/gold palette rather than the purple Accent the other buttons use. -->
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource WarningText}"/>
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Danger: red background for destructive actions -->
            <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ButtonBase}">
                <Setter Property="Background" Value="{DynamicResource Danger}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="White"/>
                <!-- Own template so the DISABLED state greys out to a neutral surface instead of
                     fading white-on-red to ~1.2:1 (illegible, esp. on light themes). Enabled/hover/
                     pressed/focus mirror ButtonBase. -->
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource RadiusMd}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter x:Name="Cp" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="1.5"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface3}"/>
                                    <Setter TargetName="Cp" Property="TextBlock.Foreground" Value="{DynamicResource TextMuted}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Default Button style — catches all unstyled buttons -->
            <Style TargetType="Button" BasedOn="{StaticResource SecondaryButton}"/>

            <!-- ============================================================ -->
            <!-- TextBox & ComboBox                                            -->
            <!-- ============================================================ -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border2}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Accent}"/>
                <Setter Property="SelectionBrush" Value="{DynamicResource Accent}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="{StaticResource RadiusMd}">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Padding="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource BorderAccent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ComboBox — theme-aware (the default WPF template uses fixed system
                 colors: a white popup + near-black text that ignore the theme, so on
                 the light presets the editable text and dropdown items rendered with
                 poor contrast). This template drives every color from the theme
                 brushes, matching the TextBox look (Surface2 field, Border2 outline,
                 CornerRadius 8, Accent focus). Covers editable + non-editable. -->
            <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                <Setter Property="Focusable" Value="False"/>
                <Setter Property="ClickMode" Value="Press"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border Background="Transparent"/>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border2}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Height" Value="34"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{StaticResource RadiusMd}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Editable text box (shown only when IsEditable) -->
                                        <TextBox x:Name="PART_EditableTextBox" Grid.Column="0"
                                                 Margin="{TemplateBinding Padding}"
                                                 VerticalContentAlignment="Center"
                                                 Background="Transparent" BorderThickness="0"
                                                 Foreground="{DynamicResource TextPrimary}"
                                                 CaretBrush="{DynamicResource Accent}"
                                                 SelectionBrush="{DynamicResource Accent}"
                                                 IsReadOnly="{TemplateBinding IsReadOnly}"
                                                 Visibility="Hidden"/>
                                        <!-- Selected item (shown when NOT editable) -->
                                        <ContentPresenter x:Name="ContentSite" Grid.Column="0"
                                                          Margin="{TemplateBinding Padding}"
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding SelectionBoxItem}"
                                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          IsHitTestVisible="False"/>

                                        <ToggleButton x:Name="ToggleChevron" Grid.Column="1"
                                                      Style="{StaticResource ComboBoxToggleButton}"
                                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"/>
                                        <TextBlock Grid.Column="1" Text="&#xE70D;" IsHitTestVisible="False"
                                                   FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" FontSize="11"
                                                   Foreground="{DynamicResource TextSecondary}"
                                                   VerticalAlignment="Center" Margin="0,0,12,0"/>
                                    </Grid>
                                </Border>

                                <Popup x:Name="PART_Popup" AllowsTransparency="True"
                                       Placement="Bottom" Focusable="False"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       PopupAnimation="Fade">
                                    <Border Background="{DynamicResource Surface1}"
                                            BorderBrush="{DynamicResource Border2}" BorderThickness="1"
                                            CornerRadius="{StaticResource RadiusMd}" Margin="0,4,0,0"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                            MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEditable" Value="True">
                                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ContentSite" Property="Visibility" Value="Collapsed"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource BorderAccent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="ComboBoxItem">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="{StaticResource RadiusSm}"
                                    Padding="{TemplateBinding Padding}" Margin="4,2">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface3}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Slider — theme-aware track (default template inherits WPF-UI's  -->
            <!-- dark dictionary; its pale rail vanished on the light presets,   -->
            <!-- leaving the thumb floating). Drives every colour from theme     -->
            <!-- brushes and keeps the PART_Track/PART_SelectionRange/Thumb part -->
            <!-- names so snapping + value binding still work.                   -->
            <!-- ============================================================ -->
            <Style TargetType="Slider">
                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                <Setter Property="Background" Value="{DynamicResource Border2}"/>
                <Setter Property="MinHeight" Value="22"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Slider">
                            <Grid VerticalAlignment="Center">
                                <Border Height="4" CornerRadius="{StaticResource RadiusPill}"
                                        Background="{TemplateBinding Background}"/>
                                <Track x:Name="PART_Track">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="Slider.DecreaseLarge" Focusable="False"
                                                      OverridesDefaultStyle="True">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Height="4" CornerRadius="{StaticResource RadiusPill}"
                                                            Background="{DynamicResource Accent}"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="Slider.IncreaseLarge" Focusable="False"
                                                      OverridesDefaultStyle="True">
                                            <RepeatButton.Template>
                                                <ControlTemplate TargetType="RepeatButton">
                                                    <Border Background="Transparent"/>
                                                </ControlTemplate>
                                            </RepeatButton.Template>
                                        </RepeatButton>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Width="16" Height="16" Focusable="False">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border Background="{DynamicResource Accent}"
                                                            CornerRadius="{StaticResource RadiusPill}"
                                                            BorderBrush="{DynamicResource Surface0}" BorderThickness="2"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- ToolTip — theme-aware surface (default inherits WPF-UI's dark   -->
            <!-- chrome; the tooltip text DID re-theme via the TextBlock style,  -->
            <!-- so on light presets it was dark text on a dark panel). Drives   -->
            <!-- both surface and text from theme brushes so they invert together.-->
            <!-- ============================================================ -->
            <Style TargetType="ToolTip">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="Background" Value="{DynamicResource Surface1}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border2}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="HasDropShadow" Value="False"/>
                <Setter Property="Padding" Value="10,7"/>
                <Setter Property="MaxWidth" Value="360"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1"
                                    CornerRadius="{StaticResource RadiusMd}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter TextBlock.Foreground="{DynamicResource TextPrimary}"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Typography helpers                                            -->
            <!-- ============================================================ -->
            <Style x:Key="Display" TargetType="TextBlock">
                <Setter Property="FontSize" Value="28"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <Style x:Key="Heading" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <Style x:Key="Caption" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <Style x:Key="Subtle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>

            <Style x:Key="Metric" TargetType="TextBlock">
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
            </Style>

            <!-- ============================================================ -->
            <!-- Side-nav tab style (vertical tabs with icon + label)          -->
            <!-- ============================================================ -->
            <Style x:Key="SideNavTabItem" TargetType="TabItem">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="MinWidth" Value="180"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Bd"
                                    Background="Transparent"
                                    CornerRadius="{StaticResource RadiusMd}"
                                    Margin="10,2"
                                    Padding="14,10">
                                <Grid>
                                    <Rectangle x:Name="ActiveMark"
                                               HorizontalAlignment="Left"
                                               Width="3" Height="20"
                                               Fill="{DynamicResource Accent}"
                                               RadiusX="2" RadiusY="2"
                                               Margin="-14,0,0,0"
                                               Visibility="Collapsed"/>
                                    <ContentPresenter ContentSource="Header"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextBlock.Foreground="{TemplateBinding Foreground}"
                                                      TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface2}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                    <Setter TargetName="ActiveMark" Property="Visibility" Value="Visible"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Pill / Badge                                                  -->
            <!-- ============================================================ -->
            <Style x:Key="Badge" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Surface3}"/>
                <Setter Property="CornerRadius" Value="{StaticResource RadiusPill}"/>
                <Setter Property="Padding" Value="10,3"/>
            </Style>

            <Style x:Key="BadgeAccent" TargetType="Border" BasedOn="{StaticResource Badge}">
                <Setter Property="Background" Value="{DynamicResource Accent}"/>
            </Style>

            <!-- ============================================================ -->
            <!-- CheckBox (dark-friendly bifă)                                 -->
            <!-- ============================================================ -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Padding" Value="8,0,0,0"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="Box" Width="18" Height="18" CornerRadius="{StaticResource RadiusSm}"
                                        Background="{DynamicResource Surface2}"
                                        BorderBrush="{DynamicResource Border2}" BorderThickness="1.5"
                                        VerticalAlignment="Center">
                                    <Path x:Name="Check"
                                          Data="M 3 9 L 7 13 L 15 5"
                                          Stroke="White" StrokeThickness="2"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          Visibility="Collapsed"
                                          SnapsToDevicePixels="True"/>
                                </Border>
                                <ContentPresenter Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                                  TextBlock.FontSize="{TemplateBinding FontSize}"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{DynamicResource BorderAccent}"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- Horizontal pill tabs (used inside Network sub-sections)      -->
            <!-- ============================================================ -->
            <Style x:Key="PillTabItem" TargetType="TabItem">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Bd"
                                    Background="Transparent"
                                    CornerRadius="{StaticResource RadiusPill}"
                                    Margin="0,0,6,0"
                                    Padding="16,7">
                                <ContentPresenter ContentSource="Header" VerticalAlignment="Center" HorizontalAlignment="Center"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"
                                                  TextBlock.FontWeight="{TemplateBinding FontWeight}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Surface2}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ============================================================ -->
            <!-- DataGrid — dark-friendly rows, headers, and cell focus        -->
            <!-- ============================================================ -->
            <Style TargetType="DataGrid">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="GridLinesVisibility" Value="None"/>
                <Setter Property="RowBackground" Value="Transparent"/>
                <Setter Property="AlternatingRowBackground" Value="{DynamicResource Surface1}"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource Border1}"/>
                <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource Border1}"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="FontSize" Value="13"/>
            </Style>

            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{DynamicResource Surface2}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="Padding" Value="12,10"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Border1}"/>
                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                <Setter Property="FontFamily" Value="{StaticResource UiFont}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridColumnHeader">
                            <Grid>
                                <Border x:Name="HeaderBorder"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Padding="{TemplateBinding Padding}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0"
                                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                          VerticalAlignment="Center"
                                                          Content="{TemplateBinding Content}"
                                                          ContentTemplate="{TemplateBinding ContentTemplate}"/>
                                        <Path x:Name="SortArrow" Grid.Column="1"
                                              Visibility="Collapsed"
                                              Fill="{DynamicResource TextSecondary}"
                                              Margin="6,0,0,0"
                                              VerticalAlignment="Center"
                                              RenderTransformOrigin="0.5,0.5"/>
                                    </Grid>
                                </Border>
                                <Thumb x:Name="PART_RightHeaderGripper" HorizontalAlignment="Right"
                                       Width="6" Cursor="SizeWE" BorderThickness="0">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="Transparent" Width="6"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="HeaderBorder" Property="Background" Value="{DynamicResource Surface3}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Trigger>
                                <Trigger Property="SortDirection" Value="Ascending">
                                    <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="SortArrow" Property="Data" Value="M 0,6 L 4,0 L 8,6 Z"/>
                                </Trigger>
                                <Trigger Property="SortDirection" Value="Descending">
                                    <Setter TargetName="SortArrow" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="SortArrow" Property="Data" Value="M 0,0 L 4,6 L 8,0 Z"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Style.Triggers>
                    <!-- Hover = subtle neutral tint; selection = accent tint. Distinct now (both were AccentSoft). -->
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{DynamicResource RowHover}"/>
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AccentSoft}"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    </Trigger>
                    <!-- Selected AND hovered: keep the accent (don't let neutral hover override it). -->
                    <MultiTrigger>
                        <MultiTrigger.Conditions>
                            <Condition Property="IsSelected" Value="True"/>
                            <Condition Property="IsMouseOver" Value="True"/>
                        </MultiTrigger.Conditions>
                        <Setter Property="Background" Value="{DynamicResource AccentSoft}"/>
                    </MultiTrigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="DataGridCell">
                            <Border Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- ============================================================ -->
            <!-- TabControl — transparent content area for dark theme          -->
            <!-- ============================================================ -->
            <Style TargetType="TabControl">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
