<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             d:DataContext="{d:DesignInstance vm:ServicesViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource Surface0}">

    <Grid Margin="28,24,28,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Services" Style="{StaticResource Display}"/>
            <TextBlock Text="Manage Windows services. Gaming recommendations highlight services safe to disable for better performance."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Elevation banner: not elevated -->
        <Border Grid.Row="1" Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border1}"
                BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="0,0,0,12"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <DockPanel>
                <Button Content="Run as administrator" Command="{Binding RelaunchAsAdminCommand}"
                        Style="{StaticResource AdminButton}" DockPanel.Dock="Right" Padding="14,8"
                        AutomationProperties.Name="Run as administrator"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE83D;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                               FontSize="16" Margin="0,0,10,0" Foreground="{DynamicResource TextSecondary}"/>
                    <TextBlock Text="Starting and stopping Windows services requires administrator privileges."
                               Foreground="{DynamicResource TextSecondary}" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Elevation banner: elevated -->
        <Border Grid.Row="1" Background="{DynamicResource WarningBgSubtle}" BorderBrush="{DynamicResource WarningBg}" BorderThickness="1"
                CornerRadius="12" Padding="12,8" Margin="0,0,0,12"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}}">
            <Grid>
                <Border Background="{DynamicResource WarningStripe}" Width="4" HorizontalAlignment="Left" CornerRadius="2"
                        Margin="-12,-8,0,-8"/>
                <StackPanel Orientation="Horizontal" Margin="6,0,0,0">
                    <TextBlock Text="&#xE83D;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource WarningText}"/>
                    <TextBlock Text="Running as administrator — you can start, stop, and configure services."
                               Foreground="{DynamicResource WarningText}" FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
            <StackPanel>
                <DockPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="Refresh" Command="{Binding RefreshCommand}"
                                Style="{StaticResource SecondaryButton}" Margin="0,0,8,0"
                                AutomationProperties.AutomationId="btn-services-refresh"
                                AutomationProperties.Name="Refresh services"/>
                        <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                                 Width="240" VerticalContentAlignment="Center"
                                 AutomationProperties.Name="Filter services"/>
                        <!-- Only shown once something is marked; see LogsView for the same pair. -->
                        <Button Content="{Binding HighlightedCount, StringFormat='Clear {0} marks'}"
                                AutomationProperties.AutomationId="btn-services-clear-marks"
                                AutomationProperties.Name="Clear all marked services"
                                Command="{Binding ClearHighlightsCommand}"
                                Style="{StaticResource GhostButton}" Margin="8,0,0,0"
                                Visibility="{Binding HighlightedCount, Converter={StaticResource FlexVis}}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                        <TextBlock Style="{StaticResource Subtle}" VerticalAlignment="Center">
                            <Run Text="{Binding RunningCount, Mode=OneWay}"/>
                            <Run Text=" running / "/>
                            <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                            <Run Text=" total"/>
                        </TextBlock>
                    </StackPanel>
                </DockPanel>
                <!-- Filter chips. All nine share one GroupName because SelectedFilter holds a single
                     value, so they are mutually exclusive by construction.
                     Five of these are new: ApplyFilter has always implemented Running, Stopped and the
                     three gaming recommendations, but no control ever selected them, so they could only
                     be reached from a debugger — while the README promised filtering "by status
                     (Running/Stopped)" and "gaming recommendation". A WrapPanel rather than the previous
                     StackPanel so nine chips reflow instead of clipping on a narrow window. -->
                <WrapPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock Text="Safety:" Foreground="{DynamicResource TextMuted}" FontSize="11"
                               VerticalAlignment="Center" Margin="0,0,8,4"/>
                    <RadioButton Content="{Binding SafeCount, StringFormat='Safe ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Safe}"
                                 Style="{StaticResource FilterChip}"
                                 AutomationProperties.Name="Filter safe services"/>
                    <RadioButton Content="{Binding CautionCount, StringFormat='Caution ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Caution}"
                                 Style="{StaticResource FilterChipCaution}"
                                 AutomationProperties.Name="Filter caution services"/>
                    <RadioButton Content="{Binding CriticalCount, StringFormat='Critical ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Critical}"
                                 Style="{StaticResource FilterChipCritical}"
                                 AutomationProperties.Name="Filter critical services"/>

                    <TextBlock Text="Status:" Foreground="{DynamicResource TextMuted}" FontSize="11"
                               VerticalAlignment="Center" Margin="10,0,8,4"/>
                    <RadioButton Content="{Binding RunningCount, StringFormat='Running ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Running}"
                                 Style="{StaticResource FilterChip}"
                                 AutomationProperties.Name="Filter running services"/>
                    <RadioButton Content="{Binding StoppedCount, StringFormat='Stopped ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Stopped}"
                                 Style="{StaticResource FilterChip}"
                                 AutomationProperties.Name="Filter stopped services"/>

                    <!-- Gaming recommendation: a different dataset from the safety level on the left.
                         Safety answers "will this break Windows"; the recommendation answers "is this
                         worth turning off for games". It is the tab's headline capability and was the
                         least reachable part of it. -->
                    <TextBlock Text="For gaming:" Foreground="{DynamicResource TextMuted}" FontSize="11"
                               VerticalAlignment="Center" Margin="10,0,8,4"/>
                    <RadioButton Content="{Binding SafeToDisableCount, StringFormat='Safe to disable ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter='Safe to disable'}"
                                 Style="{StaticResource FilterChip}"
                                 ToolTip="Services SysManager considers safe to turn off for gaming"
                                 AutomationProperties.Name="Filter services safe to disable for gaming"/>
                    <RadioButton Content="{Binding KeepEnabledCount, StringFormat='Keep enabled ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter='Keep enabled'}"
                                 Style="{StaticResource FilterChip}"
                                 ToolTip="Services to leave alone — turning these off causes problems"
                                 AutomationProperties.Name="Filter services to keep enabled"/>
                    <RadioButton Content="{Binding AdvancedCount, StringFormat='Advanced ({0})'}"
                                 GroupName="SafetyFilter" Margin="0,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=Advanced}"
                                 Style="{StaticResource FilterChip}"
                                 ToolTip="Only worth changing if you know what the service does"
                                 AutomationProperties.Name="Filter advanced services"/>

                    <RadioButton Content="All" GroupName="SafetyFilter" Margin="10,0,6,4"
                                 IsChecked="{Binding SelectedFilter, Converter={StaticResource IsEqual}, ConverterParameter=All}"
                                 Style="{StaticResource FilterChip}"
                                 AutomationProperties.Name="Show all services"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Services list — wrapped in a Card so the data grid gets the same rim-bordered
             elevation as the sibling grid views (Process Manager, Startup, Uninstaller, …). -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0">
        <Grid>
        <DataGrid AutomationProperties.Name="Windows services" ItemsSource="{Binding Services}"
                  SelectedItem="{Binding SelectedService}"
                  AutoGenerateColumns="False" HeadersVisibility="Column"
                  Background="Transparent" BorderThickness="0"
                  GridLinesVisibility="None" IsReadOnly="True"
                  SelectionMode="Single" CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <!-- Marked rows keep a tint through hover and selection; BasedOn so the app-wide
                 DataGridRow style (hover, selection, the selected+hovered MultiTrigger) is inherited
                 rather than replaced. See LogsView for the same pair. -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                            <Setter Property="Background" Value="{DynamicResource WarningBgSubtle}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <!-- Mark column — the entry point ToggleHighlightCommand never had. Same glyph Button
                     and command routing as the Actions column below and as LogsView's mark column. -->
                <DataGridTemplateColumn Header="" Width="46" MinWidth="46" CanUserSort="False" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource GhostButton}" Padding="6,2"
                                    Command="{Binding DataContext.ToggleHighlightCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    AutomationProperties.Name="{Binding DisplayName, StringFormat='Mark or unmark this service: {0}'}"
                                    ToolTip="Mark this service so it stays easy to find while you scroll"
                                    VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets" FontSize="14">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="&#xEB4F;"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsHighlighted}" Value="True">
                                                    <Setter Property="Text" Value="&#xEB4C;"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource WarningStripe}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Name" Binding="{Binding DisplayName}" Width="200" MinWidth="120" SortMemberPath="DisplayName">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="ToolTip" Value="{Binding DisplayName}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Service" Binding="{Binding Name}" Width="160" MinWidth="100" SortMemberPath="Name">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <Setter Property="ToolTip" Value="{Binding Name}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80" MinWidth="70" SortMemberPath="Status"/>
                <DataGridTextColumn Header="Startup" Binding="{Binding StartType}" Width="90" MinWidth="80" SortMemberPath="StartType"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*" MinWidth="100" SortMemberPath="Description">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                            <!-- Microsoft's own Description text is dense jargon. The app already carries
                                 25 hand-written plain-language explanations (ServiceManagerService.GamingGuide
                                 -> RecommendationReason) and nothing displayed them, so the friendly copy that
                                 makes this tab usable was shipping dead. Shown on hover where one exists;
                                 services outside the guide fall back to Microsoft's description. -->
                            <Setter Property="ToolTip" Value="{Binding RecommendationReason}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding RecommendationReason}" Value="">
                                    <Setter Property="ToolTip" Value="{Binding Description}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Safety" Width="90" MinWidth="80" SortMemberPath="SafetyLevel">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="6,3"
                                    Background="{Binding SafetyLevel, Converter={StaticResource SafetyBg}}"
                                    BorderBrush="{Binding SafetyLevel, Converter={StaticResource SafetyBg}}"
                                    BorderThickness="1" HorizontalAlignment="Left"
                                    ToolTip="{Binding SafetyDescription}">
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="6" Height="6" VerticalAlignment="Center" Margin="0,0,4,0"
                                             Fill="{Binding SafetyLevel, Converter={StaticResource SafetyBrush}}"/>
                                    <TextBlock Text="{Binding SafetyLevel, Converter={StaticResource SafetyText}}"
                                               FontSize="10" FontWeight="SemiBold"
                                               Foreground="{Binding SafetyLevel, Converter={StaticResource SafetyBrush}}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Actions" Width="280" MinWidth="280" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Start" Padding="8,2" Margin="0,0,4,0"
                                        Style="{StaticResource GhostButton}"
                                        Command="{Binding DataContext.StartServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='Start {0}'}"/>
                                <Button Content="Stop" Padding="8,2" Margin="0,0,4,0"
                                        Style="{StaticResource GhostButton}"
                                        Command="{Binding DataContext.StopServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='Stop {0}'}"/>
                                <Button Content="Disable" Padding="8,2" Margin="0,0,4,0"
                                        Style="{StaticResource GhostButton}"
                                        Command="{Binding DataContext.DisableServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='Disable {0}'}"/>
                                <Button Content="Enable" Padding="8,2"
                                        Style="{StaticResource GhostButton}"
                                        Command="{Binding DataContext.EnableServiceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        AutomationProperties.Name="{Binding DisplayName, StringFormat='Enable {0}'}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

            <!-- Empty state -->
            <TextBlock Text="No services to show. If this looks wrong, use Refresh."
                       Style="{StaticResource Subtle}" TextWrapping="Wrap" MaxWidth="360"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="{Binding Services.Count, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>
        </Grid>
        </Border>

        <!-- Status bar -->
        <DockPanel Grid.Row="4" Margin="0,8,0,0">
            <ProgressBar AutomationProperties.Name="Service list progress" DockPanel.Dock="Left" Width="120" Height="4" Margin="0,0,12,0"
                         IsIndeterminate="{Binding IsProgressIndeterminate}"
                         Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}"/>
        </DockPanel>
    </Grid>
</UserControl>
