<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.DeepCleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views">

    <Grid Background="{DynamicResource Surface0}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28,24,28,28">

                <!-- Header -->
                <DockPanel>
                    <StackPanel>
                        <TextBlock Text="Deep Cleanup" Style="{StaticResource Display}"/>
                        <TextBlock Text="Find safe-to-remove installer leftovers and caches. Everything is scan-first — nothing is deleted until you tick it and press Clean. Looking for your biggest files instead? That is Storage &amp; Files → Large Files."
                                   Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="820" HorizontalAlignment="Left"/>
                    </StackPanel>
                </DockPanel>

                <!-- Elevation. Directly under the header, which is where the golden admin-control contract
                     puts it on every other privileged tab, and Margin="0,12,0,0" because this is a
                     scrolling page whose StackPanel already carries the 28 gutter.
                     Six of the buckets live under %WinDir% — the Windows Update download cache, Delivery
                     Optimization, the Installer patch cache, Windows\Temp, Prefetch and the blue-screen
                     memory dumps — and without admin they were silently counted as "skipped" with no
                     stated reason. -->
                <v:AdminBanner Margin="0,12,0,0"
                               NotElevatedMessage="Some of the biggest items — the Windows Update download cache, Delivery Optimization, the Installer patch cache, Windows\Temp, Prefetch and the blue-screen memory dumps — can only be deleted as administrator. They are still scanned and counted, and shown as skipped."
                               ElevatedMessage="Running as administrator — the Windows Update, Delivery Optimization, Installer patch cache, Windows\Temp, Prefetch and blue-screen memory dump items can be deleted too."/>

                <!-- Safety note -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0"
                        Background="{DynamicResource AccentSoft}" BorderBrush="{DynamicResource Accent}" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                   FontSize="18" Foreground="{DynamicResource Accent}" VerticalAlignment="Top" Margin="0,2,12,0"/>
                        <StackPanel>
                            <TextBlock Text="Safe by design" FontWeight="SemiBold" Foreground="{DynamicResource Accent}"/>
                            <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextSecondary}" MaxWidth="900" Margin="0,4,0,0" FontSize="12">
                                Only locations documented as safe by vendors (NVIDIA, AMD, Microsoft) are scanned. We never touch browser caches, passwords,
                                live app settings, the registry, active drivers, or Program Files. A file that's locked or in use is skipped — nothing is forced.
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- SCAN + CLEAN -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="Safe-to-clean categories" Style="{StaticResource SectionTitle}"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                                <Button Content="Scan" Command="{Binding ScanCommand}" Style="{StaticResource PrimaryButton}"
                                        IsEnabled="{Binding IsScanning, Converter={StaticResource BoolInvert}}"/>
                                <Button Content="Select all safe" Command="{Binding SelectAllCommand}" CommandParameter="{StaticResource TrueValue}"
                                        Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                                <Button Content="Deselect All" Command="{Binding SelectAllCommand}" CommandParameter="{StaticResource FalseValue}"
                                        Style="{StaticResource GhostButton}" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DockPanel>
                        <TextBlock Text="{Binding ScanSummary}" Style="{StaticResource Subtle}" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <!-- Scan progress — determinate, category-by-category -->
                        <StackPanel Margin="0,0,0,10" Visibility="{Binding IsScanning, Converter={StaticResource FlexVis}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <!-- Announced: this is the coarse "what is happening" line. The percentage
                                     beside it deliberately is not — a live region on a number that changes
                                     every tick produces continuous speech instead of information. -->
                                <TextBlock Grid.Column="0" Text="{Binding ScanStatusLine}" Foreground="{DynamicResource Accent}" FontWeight="SemiBold" VerticalAlignment="Center"
                                           AutomationProperties.LiveSetting="Polite"/>
                                <TextBlock Grid.Column="1" Text="{Binding ScanProgress, StringFormat={}{0}%}" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </Grid>
                            <ProgressBar AutomationProperties.Name="Scan progress" Height="6" Minimum="0" Maximum="100" Value="{Binding ScanProgress}" Margin="0,6,0,0"/>
                            <!-- The VM already runs an EtaCalculator over ScanProgress; nothing bound the
                                 result, so the estimate for a scan that can take minutes was discarded. -->
                            <TextBlock Text="{Binding ScanEtaText}" Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,4,0,0"
                                       Visibility="{Binding ScanEtaText, Converter={StaticResource FlexVis}}"/>
                        </StackPanel>

                        <!-- Before the first scan Categories is empty, and this card is otherwise a heading
                             over blank space. ScanSummary above says what to do, but it is always visible
                             and so is not an empty state — this is. The gap was masked while the large-files
                             card below carried the view's only <v:EmptyState/>, since the guard that checks
                             for one reads the whole file; the card left in #1523 and the gap surfaced. -->
                        <v:EmptyState Glyph="&#xE74D;"
                                      Title="Nothing scanned yet"
                                      Message="Press Scan above and SysManager will list what it found, with sizes, so you can pick what goes."
                                      Visibility="{Binding Categories.Count, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}"/>

                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardInner}" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" AutomationProperties.Name="{Binding Name, StringFormat='Select {0} for cleanup'}" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}"/>
                                                    <Border Margin="10,0,0,0" Padding="8,2" CornerRadius="{StaticResource RadiusPill}"
                                                            Background="{DynamicResource DangerBgSubtle}" BorderBrush="{DynamicResource DangerBorder}" BorderThickness="1"
                                                            Visibility="{Binding IsDestructiveHint, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="Irreversible" Foreground="{DynamicResource DangerText}" FontSize="11" FontWeight="SemiBold"/>
                                                    </Border>
                                                </StackPanel>
                                                <TextBlock Text="{Binding Description}" Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap" MaxWidth="820"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="10,0,0,0">
                                                <TextBlock Text="{Binding SizeDisplay}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource TextPrimary}" HorizontalAlignment="Right"/>
                                                <TextBlock Text="{Binding CountDisplay}" Style="{StaticResource Caption}" HorizontalAlignment="Right"/>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Clean action row -->
                        <Border Style="{StaticResource CardInner}" Margin="0,8,0,0" Background="{DynamicResource Surface2}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Selected total:" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding TotalSelectedDisplay}" Style="{StaticResource MetricCompact}"
                                               Foreground="{DynamicResource Accent}" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal">
                                    <Button Content="Clean selected" Command="{Binding CleanCommand}"
                                            Style="{StaticResource DangerButton}"/>
                                    <Button Content="Cancel" AutomationProperties.Name="Cancel cleanup" Command="{Binding CancelCommand}" Style="{StaticResource GhostButton}" Margin="8,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <TextBlock Text="{Binding CleanSummary}" Style="{StaticResource Subtle}" Margin="0,8,0,0" TextWrapping="Wrap"
                                   Visibility="{Binding CleanSummary, Converter={StaticResource FlexVis}}"/>

                        <!-- Clean progress — determinate, category-by-category -->
                        <StackPanel Margin="0,8,0,0" Visibility="{Binding IsCleaning, Converter={StaticResource FlexVis}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding CleanStatusLine}" Foreground="{DynamicResource Success}" FontWeight="SemiBold" VerticalAlignment="Center"
                                           AutomationProperties.LiveSetting="Polite"/>
                                <TextBlock Grid.Column="1" Text="{Binding CleanProgress, StringFormat={}{0}%}" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" Margin="12,0,0,0"/>
                            </Grid>
                            <ProgressBar AutomationProperties.Name="Cleanup progress" Height="6" Minimum="0" Maximum="100" Value="{Binding CleanProgress}" Margin="0,6,0,0"/>
                            <!-- Same for the clean phase, which deletes real files — knowing how long is
                                 left is what stops someone killing the app mid-delete. -->
                            <TextBlock Text="{Binding CleanEtaText}" Foreground="{DynamicResource TextMuted}" FontSize="11" Margin="0,4,0,0"
                                       Visibility="{Binding CleanEtaText, Converter={StaticResource FlexVis}}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
