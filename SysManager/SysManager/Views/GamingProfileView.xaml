<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.GamingProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:GamingProfileViewModel}"
             Background="{DynamicResource Surface0}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Preview banner -->
        <v:DevelopmentBanner Grid.Row="0" Margin="28,24,28,0"/>

        <!-- Header -->
        <StackPanel Grid.Row="1" Margin="28,16,28,0">
            <TextBlock Text="Gaming Profile" Style="{StaticResource Display}"/>
            <TextBlock Text="One-click game mode: apply a bundle of reversible optimizations, optionally targeting a specific game so its CPU priority and core affinity are tuned. Everything is restored automatically when the game exits, or when you press Stop. Preview: this first version is fully reversible — closing background apps and saved per-game profiles are coming in a later update."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap"
                       MaxWidth="820" HorizontalAlignment="Left"/>
        </StackPanel>

        <!-- Admin banner (some optimizations need elevation) -->
        <Border Grid.Row="2" Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border1}"
                BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="28,12,28,0"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <DockPanel>
                <Button Content="Run as administrator" Command="{Binding RelaunchAsAdminCommand}"
                        Style="{StaticResource AdminButton}" DockPanel.Dock="Right" Padding="14,8"
                        AutomationProperties.Name="Run as administrator"/>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="&#xE7EF;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                               FontSize="16" Margin="0,0,10,0" Foreground="{DynamicResource TextSecondary}"/>
                    <TextBlock Text="Most optimizations apply as-is. Freeing standby memory and pausing search indexing need administrator."
                               Foreground="{DynamicResource TextSecondary}" FontSize="13" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Elevation banner: elevated (golden admin contract, matches AppBlockerView) -->
        <Border Grid.Row="2" Background="{DynamicResource WarningBgSubtle}" BorderBrush="{DynamicResource WarningBg}" BorderThickness="1"
                CornerRadius="12" Padding="12,8" Margin="28,12,28,0"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}}">
            <Grid>
                <Border Background="{DynamicResource WarningStripe}" Width="4" HorizontalAlignment="Left" CornerRadius="2"
                        Margin="-12,-8,0,-8"/>
                <StackPanel Orientation="Horizontal" Margin="6,0,0,0">
                    <TextBlock Text="&#x1F6E1;" FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock Text="Running as administrator — every optimization, including freeing standby memory and pausing search indexing, can be applied."
                               Foreground="{DynamicResource WarningText}" FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center" TextWrapping="Wrap"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content: game target + optimization toggles -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="28,16,28,0">
            <StackPanel>
                <!-- Game target (optional) -->
                <Border Style="{StaticResource Card}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="GAME (OPTIONAL)" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
                        <TextBlock Text="Pick the running game to tune its CPU priority and core affinity, and to auto-revert when it exits. Leave empty to apply only the system-wide optimizations."
                                   Style="{StaticResource Subtle}" Margin="0,0,0,10" TextWrapping="Wrap"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox Grid.Column="0" ItemsSource="{Binding Processes}"
                                      SelectedItem="{Binding SelectedGame}" DisplayMemberPath="Name"
                                      IsEnabled="{Binding IsSessionActive, Converter={StaticResource BoolInvert}}"
                                      AutomationProperties.Name="Game process to optimize"/>
                            <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshProcessesCommand}"
                                    Style="{StaticResource GhostButton}" Margin="8,0,0,0"
                                    AutomationProperties.Name="Refresh the running-process list"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Optimizations -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="OPTIMIZATIONS" Style="{StaticResource SectionLabel}" Margin="0,0,0,4"/>
                        <TextBlock Text="Tick the tweaks to apply. Each is fully reversible; system-wide ones are restored to their exact previous state on Stop or game exit."
                                   Style="{StaticResource Subtle}" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <CheckBox Content="Ultimate Performance power plan" IsChecked="{Binding UltimatePerformancePlan}"
                                  Margin="0,4" FontWeight="SemiBold"
                                  AutomationProperties.Name="Switch to the Ultimate Performance power plan"/>
                        <CheckBox Content="Reduce visual effects (animations, fades, shadows)" IsChecked="{Binding DisableVisualEffects}"
                                  Margin="0,4" FontWeight="SemiBold"
                                  AutomationProperties.Name="Reduce visual effects"/>
                        <CheckBox Content="Finest timer resolution (~0.5 ms)" IsChecked="{Binding FinestTimerResolution}"
                                  Margin="0,4" FontWeight="SemiBold"
                                  AutomationProperties.Name="Request the finest timer resolution"/>
                        <CheckBox Content="Silence notifications while gaming" IsChecked="{Binding SilenceNotifications}"
                                  Margin="0,4" FontWeight="SemiBold"
                                  AutomationProperties.Name="Silence notifications while gaming"/>

                        <!-- Admin-gated system optimizations -->
                        <CheckBox Content="Free standby memory (needs administrator)" IsChecked="{Binding PurgeStandbyMemory}"
                                  Margin="0,4" FontWeight="SemiBold" IsEnabled="{Binding IsElevated}"
                                  AutomationProperties.Name="Free standby memory (requires administrator)"/>
                        <CheckBox Content="Pause Windows Search indexing (needs administrator)" IsChecked="{Binding PauseSearchIndexing}"
                                  Margin="0,4" FontWeight="SemiBold" IsEnabled="{Binding IsElevated}"
                                  AutomationProperties.Name="Pause Windows Search indexing (requires administrator)"/>

                        <!-- Per-game optimizations (only meaningful with a game selected) -->
                        <Border Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border1}"
                                BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,10,0,0"
                                IsEnabled="{Binding HasSelectedGame}">
                            <StackPanel>
                                <TextBlock Text="Applied to the selected game" Style="{StaticResource Caption}" Margin="0,0,0,6"/>
                                <CheckBox Content="High CPU priority for the game" IsChecked="{Binding HighGameCpuPriority}"
                                          Margin="0,4" FontWeight="SemiBold"
                                          AutomationProperties.Name="Set the game to high CPU priority"/>
                                <CheckBox Content="Pin the game to performance cores" IsChecked="{Binding PinGameToPerformanceCores}"
                                          Margin="0,4" FontWeight="SemiBold"
                                          AutomationProperties.Name="Pin the game to performance cores"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action bar -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="28,12,28,24" Padding="12">
            <DockPanel>
                <ProgressBar AutomationProperties.Name="Working" DockPanel.Dock="Left" Width="100" Height="4"
                             VerticalAlignment="Center" Margin="0,0,12,0"
                             IsIndeterminate="{Binding IsProgressIndeterminate}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center"
                           Text="{Binding StatusMessage}" Style="{StaticResource Caption}" TextWrapping="Wrap"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Start game mode" Command="{Binding StartCommand}" Style="{StaticResource PrimaryButton}"
                            Margin="0,0,8,0" Padding="16,8" AutomationProperties.Name="Start game mode"/>
                    <Button Content="Stop" Command="{Binding StopCommand}" Style="{StaticResource SecondaryButton}"
                            Padding="16,8" AutomationProperties.Name="Stop game mode and restore settings"/>
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
