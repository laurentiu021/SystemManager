<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.DarkModeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:DarkModeViewModel}"
             Background="{DynamicResource Surface0}">

    <Grid Margin="28,24,28,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Preview banner -->
        <v:DevelopmentBanner Grid.Row="0" Margin="0,0,0,16"/>

        <!-- Header -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Dark Mode Scheduler" Style="{StaticResource Display}"/>
            <TextBlock Text="Switch the Windows light/dark theme, or have it follow a schedule. The schedule runs while SysManager (or its tray) is open."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Manual switch -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Switch now" Style="{StaticResource SectionTitle}"/>
                <WrapPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Dark" Command="{Binding SwitchToDarkCommand}"
                            Style="{StaticResource PrimaryButton}" Margin="0,0,8,0" Padding="20,8"
                            AutomationProperties.Name="Switch to dark theme"/>
                    <Button Content="Light" Command="{Binding SwitchToLightCommand}"
                            Style="{StaticResource SecondaryButton}" Margin="0,0,16,0" Padding="20,8"
                            AutomationProperties.Name="Switch to light theme"/>
                    <CheckBox Content="Also switch taskbar &amp; Start" IsChecked="{Binding ApplyToSystem}"
                              VerticalAlignment="Center"
                              AutomationProperties.Name="Also switch the system theme"/>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Schedule -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <CheckBox Content="Automatically switch on a schedule" IsChecked="{Binding ScheduleEnabled}"
                          FontWeight="SemiBold" AutomationProperties.Name="Enable the dark-mode schedule"/>
                <Grid Margin="0,12,0,0" IsEnabled="{Binding ScheduleEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="DARK STARTS AT" Style="{StaticResource Caption}"/>
                        <TextBox Text="{Binding DarkStart, UpdateSourceTrigger=LostFocus}" Padding="8,6" Margin="0,4,0,0"
                                 FontSize="14" AutomationProperties.Name="Dark theme start time (HH:mm)"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="LIGHT STARTS AT" Style="{StaticResource Caption}"/>
                        <TextBox Text="{Binding LightStart, UpdateSourceTrigger=LostFocus}" Padding="8,6" Margin="0,4,0,0"
                                 FontSize="14" AutomationProperties.Name="Light theme start time (HH:mm)"/>
                    </StackPanel>
                </Grid>
                <TextBlock Text="Use 24-hour times, e.g. 19:00 and 07:00." Style="{StaticResource Caption}" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Spacer -->
        <Border Grid.Row="4"/>

        <!-- Status -->
        <TextBlock Grid.Row="5" Text="{Binding StatusMessage}" Style="{StaticResource Caption}"
                   Margin="0,12,0,0" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
