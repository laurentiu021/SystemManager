<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.StandbyMemoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:SysManager.Views"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:StandbyMemoryViewModel}"
             Background="{DynamicResource Surface0}">

    <Grid Margin="28,24,28,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Preview banner -->

        <!-- Header -->
        <StackPanel Grid.Row="1" Margin="0,0,0,16">
            <TextBlock Text="Standby List Cleaner" Style="{StaticResource Display}"/>
            <TextBlock Text="Free cached standby memory to reduce stutter when RAM runs low. Safe — cached data is reloaded from disk on next use; nothing is lost."
                       Style="{StaticResource Subtle}" Margin="0,4,0,0" TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Elevation banner -->
        <Border Grid.Row="2" Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border1}"
                BorderThickness="1" CornerRadius="8" Padding="14,12" Margin="0,0,0,12"
                Visibility="{Binding IsElevated, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
            <DockPanel>
                <Button Content="Run as administrator" Command="{Binding RelaunchAsAdminCommand}"
                        Style="{StaticResource AdminButton}" DockPanel.Dock="Right" Padding="14,8"
                        AutomationProperties.Name="Restart as administrator"/>
                <TextBlock Text="Purging the standby list requires administrator privileges."
                           Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </DockPanel>
        </Border>

        <!-- Memory cards -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardInner}" Margin="0,0,4,0">
                    <StackPanel>
                        <TextBlock Text="TOTAL RAM" Style="{StaticResource Caption}"/>
                        <TextBlock Text="{Binding TotalDisplay}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextSecondary}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardInner}" Margin="4,0,4,0">
                    <StackPanel>
                        <TextBlock Text="AVAILABLE" Style="{StaticResource Caption}"/>
                        <TextBlock Text="{Binding AvailableDisplay}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Success}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource CardInner}" Margin="4,0,0,0">
                    <StackPanel>
                        <TextBlock Text="MEMORY LOAD" Style="{StaticResource Caption}"/>
                        <TextBlock Text="{Binding LoadDisplay}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Info}" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Auto-purge -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="0,0,0,12">
            <StackPanel>
                <CheckBox Content="Automatically purge when available RAM is low"
                          IsChecked="{Binding AutoPurgeEnabled}" IsEnabled="{Binding IsElevated}"
                          FontWeight="SemiBold" AutomationProperties.Name="Enable automatic standby purge"/>
                <Grid Margin="0,12,0,0" IsEnabled="{Binding AutoPurgeEnabled}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Threshold" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondary}" Margin="0,0,12,0"/>
                    <Slider Grid.Column="1" Minimum="256" Maximum="8192" Value="{Binding ThresholdMb}"
                            TickFrequency="256" IsSnapToTickEnabled="True" VerticalAlignment="Center"
                            AutomationProperties.Name="Auto-purge threshold in megabytes"/>
                    <TextBlock Grid.Column="2" Text="{Binding ThresholdMb, StringFormat={}{0:F0} MB}"
                               VerticalAlignment="Center" Margin="12,0,0,0" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextSecondary}"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Spacer -->
        <Border Grid.Row="5"/>

        <!-- Actions + status -->
        <Grid Grid.Row="6" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <DockPanel Grid.Column="0" VerticalAlignment="Center">
                <ProgressBar AutomationProperties.Name="Progress" DockPanel.Dock="Left" Width="120" Height="4" Margin="0,0,12,0"
                             IsIndeterminate="{Binding IsProgressIndeterminate}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}" TextWrapping="Wrap"/>
            </DockPanel>
            <Button Grid.Column="1" Content="Purge standby list" Command="{Binding PurgeCommand}"
                    Style="{StaticResource PrimaryButton}" Padding="16,8" IsEnabled="{Binding IsElevated}"
                    AutomationProperties.Name="Purge the standby list now"/>
        </Grid>
    </Grid>
</UserControl>
