<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.DriversView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SysManager.ViewModels"
             xmlns:v="clr-namespace:SysManager.Views"
             d:DataContext="{d:DesignInstance vm:DriversViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource Surface0}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="28,24,28,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Drivers" Style="{StaticResource Display}"/>
                <TextBlock Text="Installed system drivers — click column headers to sort."
                           Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Toolbar -->
            <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
                <WrapPanel>
                    <Button Content="List drivers" Command="{Binding ListDriversCommand}"
                            AutomationProperties.AutomationId="btn-drivers-list"
                            AutomationProperties.Name="List drivers"
                            Style="{StaticResource PrimaryButton}" Margin="0,0,8,0"/>
                    <Button Content="Cancel" Command="{Binding CancelCommand}"
                            Style="{StaticResource GhostButton}" Margin="0,0,8,0"/>
                    <CheckBox Content="Hide built-in Windows drivers" IsChecked="{Binding HideSystemDrivers}"
                              VerticalAlignment="Center" Margin="8,0,0,0"
                              AutomationProperties.Name="Hide built-in Windows drivers"
                              ToolTip="Hide the drivers that come with Windows, leaving the ones your hardware maker installed (graphics, audio, network, and so on)."/>
                </WrapPanel>
            </Border>

            <!-- Summary -->
            <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="12">
                <TextBlock Text="{Binding Summary}" FontWeight="SemiBold" FontSize="13"/>
            </Border>

            <!-- Driver table -->
            <Border Grid.Row="3" Style="{StaticResource Card}" Padding="0" MinHeight="200" MaxHeight="600">
                <Grid>
                <DataGrid AutomationProperties.Name="Installed drivers" ItemsSource="{Binding Drivers}"
                          AutoGenerateColumns="False" HeadersVisibility="Column"
                          Background="Transparent" BorderThickness="0"
                          GridLinesVisibility="None" IsReadOnly="True"
                          SelectionMode="Single" CanUserSortColumns="True"
                      CanUserResizeColumns="True"
                          VerticalScrollBarVisibility="Auto"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Device Name" Binding="{Binding DeviceName}" Width="*" MinWidth="120"
                                            SortMemberPath="DeviceName" SortDirection="Ascending">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="FontSize" Value="13"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="180" MinWidth="80"
                                            SortMemberPath="Manufacturer">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Version" Binding="{Binding DriverVersion}" Width="140" MinWidth="80"
                                            SortMemberPath="DriverVersion">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Date" Binding="{Binding DriverDateDisplay}" Width="100" MinWidth="80"
                                            SortMemberPath="DriverDate">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontFamily" Value="Consolas"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextMuted}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <!-- Nothing scanned yet -->
                <v:EmptyState Glyph="&#xE950;"
                              Title="No drivers listed"
                              Message="Click 'List drivers' to enumerate installed system drivers."
                              Visibility="{Binding HasNotScanned, Converter={StaticResource FlexVis}}"/>
                <!-- Scanned, but the filter hid every driver: telling the user to scan again would be wrong. -->
                <v:EmptyState Glyph="&#xE721;"
                              Title="No drivers match your filter"
                              Message="Every driver on this PC is supplied by Windows. Untick 'Hide built-in Windows drivers' to see them."
                              Visibility="{Binding HasNoResults, Converter={StaticResource FlexVis}}"/>
                </Grid>
            </Border>

            <!-- Status bar -->
            <DockPanel Grid.Row="4" Margin="0,8,0,0">
                <ProgressBar AutomationProperties.Name="Driver scan progress" DockPanel.Dock="Left" Width="120" Height="4" Margin="0,0,12,0"
                             IsIndeterminate="{Binding IsProgressIndeterminate}"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Caption}"/>
            </DockPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
