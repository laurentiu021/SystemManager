<!-- SysManager · Author: laurentiu021 · https://github.com/laurentiu021/SystemManager · License: MIT -->
<UserControl x:Class="SysManager.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:SysManager.Helpers">

    <Grid Background="{DynamicResource Surface0}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="28,24,28,28">

                <!-- Header -->
                <StackPanel>
                    <TextBlock Text="About SysManager" Style="{StaticResource Display}"/>
                    <TextBlock Text="Version info, updates and release notes." Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
                    <TextBlock Text="Built by laurentiu021 · MIT licensed" Style="{StaticResource Subtle}" Margin="0,2,0,0" Opacity="0.7"/>
                </StackPanel>

                <!-- Version card -->
                <Border Style="{StaticResource Card}" Margin="0,20,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <!-- Report / environment status line, added below the button row. -->
                            <RowDefinition Height="Auto"/>
                            <!-- Startup update-check preference. -->
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="SysManager" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                <Border Style="{StaticResource BadgeAccent}" Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CurrentVersion, StringFormat=v{0}}" Foreground="White" FontWeight="SemiBold" FontSize="12"/>
                                </Border>
                            </StackPanel>
                            <TextBlock Text="Local Windows system monitoring toolkit." Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                                <TextBlock Text="BUILD" Style="{StaticResource SectionLabel}"/>
                                <TextBlock Text="{Binding BuildDate}" Margin="8,0,24,0" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                                <TextBlock Text="LICENSE" Style="{StaticResource SectionLabel}"/>
                                <TextBlock Text="MIT" Margin="8,0,24,0" Foreground="{DynamicResource TextSecondary}" FontSize="12"/>
                                <TextBlock Text="SOURCE" Style="{StaticResource SectionLabel}"/>
                                <TextBlock Margin="8,0,0,0" FontSize="12">
                                    <Hyperlink Command="{Binding OpenRepoCommand}" Foreground="{DynamicResource Accent}">github.com/laurentiu021/SystemManager</Hyperlink>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Check for updates" Command="{Binding CheckForUpdatesCommand}"
                                    Style="{StaticResource PrimaryButton}"
                                    IsEnabled="{Binding IsCheckingForUpdates, Converter={StaticResource BoolInvert}}"/>
                            <Button Content="Export full report"
                                    Command="{Binding ExportToFileCommand}"
                                    Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"
                                    IsEnabled="{Binding IsGeneratingReport, Converter={StaticResource BoolInvert}}"
                                    ToolTip="Generate a comprehensive system report and choose where to save it."/>
                            <Button Content="Copy full report"
                                    Command="{Binding CopyReportCommand}"
                                    Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"
                                    IsEnabled="{Binding IsGeneratingReport, Converter={StaticResource BoolInvert}}"
                                    ToolTip="Generate a comprehensive system report and copy it to the clipboard."/>
                            <Button Content="Copy environment info"
                                    Command="{Binding CopyEnvironmentInfoCommand}"
                                    Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"
                                    ToolTip="Copy your SysManager version, Windows version, architecture and elevation state — ready to paste into a bug report."/>
                            <!-- Only appears when a previous build was actually retained, so this is never
                                 a button that cannot do anything. The updater used to be the one mutation
                                 in the app with no way back: the atomic move that makes an INTERRUPTED
                                 update safe also destroyed the outgoing executable, so a SUCCESSFUL
                                 update into a broken build left nothing to return to. -->
                            <Button Content="{Binding RollBackLabel}" Command="{Binding RollBackCommand}"
                                    Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"
                                    Visibility="{Binding CanRollBack, Converter={StaticResource BoolToVis}}"
                                    AutomationProperties.Name="Go back to the previous version of SysManager"
                                    ToolTip="Reinstall the version you had before the last update. SysManager will close and reopen; your settings are not affected."/>
                            <Button Content="View license" Command="{Binding OpenLicenseCommand}"
                                    Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"/>
                        </StackPanel>

                        <!-- Report / environment results live here, next to the buttons that produce
                             them. They previously wrote to UpdateStatus inside the update card below,
                             so exporting a report replaced "Update available: vX.Y.Z" with
                             "Report saved" — losing the more important message and making the update
                             card describe something unrelated. Collapsed until there is something
                             to say, so the row does not reserve empty space. -->
                        <StackPanel Grid.Row="1" Grid.ColumnSpan="2">
                            <TextBlock Text="{Binding ReportStatus}"
                                       Style="{StaticResource Subtle}" Margin="0,10,0,0" TextWrapping="Wrap"
                                       Visibility="{Binding ReportStatus, Converter={StaticResource FlexVis}, Mode=OneWay}"/>
                            <!-- Rollback feedback lives beside its own button rather than in the update
                                 card below, for the same reason ReportStatus was moved here: writing to a
                                 shared status line makes one action overwrite another's message. -->
                            <TextBlock Text="{Binding RollBackStatus}"
                                       Style="{StaticResource Subtle}" Margin="0,10,0,0" TextWrapping="Wrap"
                                       Visibility="{Binding RollBackStatus, Converter={StaticResource FlexVis}, Mode=OneWay}"/>
                        </StackPanel>

                        <!-- The app's only outbound call, now something the user can see and switch
                             off. Same shape and wording style as Bulk Installer's "Load app icons
                             from the web" toggle: plain language, plus a tooltip naming exactly what
                             does and does not leave the machine. Unchecking does NOT disable the
                             "Check for updates" button above — that stays available on demand. -->
                        <CheckBox Grid.Row="2" Grid.ColumnSpan="2" Margin="0,10,0,0"
                                  Content="Check GitHub for a new version when SysManager starts"
                                  IsChecked="{Binding CheckForUpdatesOnStartup, Mode=TwoWay}"
                                  AutomationProperties.Name="Check GitHub for a new version when SysManager starts"
                                  ToolTip="On by default, so you hear about fixes. SysManager asks GitHub's public releases page which version is newest — nothing about you or your PC is sent. Turn it off and the check runs only when you press Check for updates."/>
                    </Grid>
                </Border>

                <!-- Update available card -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0"
                        BorderBrush="{DynamicResource Accent}" BorderThickness="1"
                        Visibility="{Binding UpdateAvailable, Converter={StaticResource FlexVis}}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE896;" FontFamily="Segoe Fluent Icons,Segoe MDL2 Assets"
                                               FontSize="18" Foreground="{DynamicResource Accent}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Text="Update available" FontSize="16" FontWeight="SemiBold"
                                               Foreground="{DynamicResource Accent}" VerticalAlignment="Center"/>
                                    <TextBlock Text="  ·  " Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LatestVersionLabel}" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                                    <TextBlock Text="  ·  " Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding LatestPublishedLabel}" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{Binding UpdateStatus}" Style="{StaticResource Subtle}" Margin="0,6,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="Download" Command="{Binding DownloadCommand}"
                                        Style="{StaticResource PrimaryButton}" Padding="18,10" FontSize="14"
                                        Visibility="{Binding ShowDownloadButton, Converter={StaticResource BoolToVis}}"/>
                                <Button Content="Install" Command="{Binding InstallUpdateCommand}"
                                        Style="{StaticResource PrimaryButton}" Padding="18,10" FontSize="14"
                                        Visibility="{Binding DownloadedPath, Converter={StaticResource FlexVis}}"/>
                                <Button Content="Manual download" Command="{Binding OpenManualDownloadCommand}"
                                        Style="{StaticResource SecondaryButton}" Margin="8,0,0,0"
                                        Visibility="{Binding AutoDownloadFailed, Converter={StaticResource FlexVis}}"/>
                            </StackPanel>
                        </Grid>

                        <!-- Download progress -->
                        <StackPanel Margin="0,14,0,0"
                                    Visibility="{Binding IsDownloading, Converter={StaticResource FlexVis}}">
                            <ProgressBar AutomationProperties.Name="Progress" Height="6" Minimum="0" Maximum="100" Value="{Binding DownloadPercent}"/>
                            <TextBlock Text="{Binding DownloadStatus}" Style="{StaticResource Subtle}" Margin="0,4,0,0"/>
                        </StackPanel>

                        <!-- Download completed -->
                        <StackPanel Margin="0,14,0,0" Orientation="Horizontal"
                                    Visibility="{Binding DownloadedPath, Converter={StaticResource FlexVis}}">
                            <Ellipse Width="10" Height="10" Fill="{DynamicResource Success}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding DownloadStatus}" Foreground="{DynamicResource Success}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                            <TextBlock Text="  ·  " Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                            <TextBlock VerticalAlignment="Center">
                                <Hyperlink Command="{Binding OpenDownloadFolderCommand}" Foreground="{DynamicResource Accent}">Show file</Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Up-to-date quiet card (or error with retry) -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0"
                        Visibility="{Binding UpdateAvailable, Converter={StaticResource FlexVis}, ConverterParameter=Inverse}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <!-- Status dot: green normally, red when the last check failed to reach GitHub,
                             so the colour agrees with the error message instead of always reading success. -->
                        <Ellipse Grid.Column="0" Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,10,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{DynamicResource Success}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding UpdateCheckFailed}" Value="True">
                                            <Setter Property="Fill" Value="{DynamicResource Danger}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Grid.Column="1" Text="{Binding UpdateStatus}" Foreground="{DynamicResource TextSecondary}"
                                   VerticalAlignment="Center" TextWrapping="Wrap"/>
                        <Button Grid.Column="2" Content="Retry" Command="{Binding CheckForUpdatesCommand}"
                                Style="{StaticResource SecondaryButton}" Padding="14,6" Margin="12,0,0,0"
                                IsEnabled="{Binding IsCheckingForUpdates, Converter={StaticResource BoolInvert}}"/>
                    </Grid>
                </Border>

                <!-- What's new — full history -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <DockPanel>
                            <TextBlock Text="What's new" Style="{StaticResource SectionTitle}"/>
                            <Button Content="Refresh" Command="{Binding LoadHistoryCommand}" Style="{StaticResource GhostButton}"
                                    HorizontalAlignment="Right" DockPanel.Dock="Right"/>
                        </DockPanel>
                        <TextBlock Text="Release notes pulled live from GitHub. Each entry shows the patch version, date and the full changelog."
                                   Style="{StaticResource Subtle}" Margin="0,4,0,10" TextWrapping="Wrap"/>

                        <ItemsControl ItemsSource="{Binding ReleaseHistory}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource CardInner}" Margin="0,0,0,10">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding Version}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimary}"/>
                                                <Border Margin="10,0,0,0" VerticalAlignment="Center"
                                                        Visibility="{Binding IsCurrent, Converter={StaticResource BoolToVis}}"
                                                        Style="{StaticResource BadgeAccent}">
                                                    <TextBlock Text="Current" Foreground="White" FontSize="11" FontWeight="SemiBold"/>
                                                </Border>
                                                <TextBlock Text="  ·  " Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                                <TextBlock Text="{Binding Title}" Foreground="{DynamicResource TextSecondary}" VerticalAlignment="Center"/>
                                                <TextBlock Text="  ·  " Foreground="{DynamicResource TextMuted}" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding PublishedAt}" Foreground="{DynamicResource TextMuted}" FontSize="12" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBlock helpers:MarkdownTextBlock.Markdown="{Binding Body}" Foreground="{DynamicResource TextSecondary}"
                                                       TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Legal footer -->
                <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                    <StackPanel>
                        <TextBlock Text="Legal" Style="{StaticResource SectionTitle}"/>
                        <TextBlock TextWrapping="Wrap" Margin="0,6,0,0" Foreground="{DynamicResource TextSecondary}" FontSize="12">
                            SysManager is released under the MIT License. The software is provided "AS IS", without warranty of any kind.
                            All operations are local to your machine — no telemetry, no accounts. Updates are fetched from the public
                            GitHub Releases API and downloaded directly from GitHub.
                        </TextBlock>
                        <TextBlock TextWrapping="Wrap" Margin="0,8,0,0" Foreground="{DynamicResource TextSecondary}" FontSize="12">
                            Some tools change system settings, the registry, or delete files. Use them at your own risk: review each
                            action before confirming, and back up important data first. Where it can, SysManager creates a System
                            Restore point before a system-altering operation and makes changes individually reversible.
                        </TextBlock>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
